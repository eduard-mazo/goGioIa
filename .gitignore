# Go
/bin/
# Paquetes de despliegue (make dist)
/dist/
*.exe
*.test
*.out

# Node / frontend
/frontend/node_modules/
/frontend/.vite/
npm-debug.log*

# Built + embedded assets (regenerate with `make build-web`)
/web/dist/*
!/web/dist/.gitkeep

# Misc
.DS_Store
*.log

# Deploy artefacts
*.tar

# Credenciales locales de desarrollo (nunca se versionan)
/deploy/dev.local.env

# Configuración de despliegue: contiene la password de Oracle. Solo se versiona
# la plantilla gogioia.env.example.
gogioia.env
.env
